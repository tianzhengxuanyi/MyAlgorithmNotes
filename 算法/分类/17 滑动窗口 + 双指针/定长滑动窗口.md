## [定长滑动窗口](https://leetcode.cn/problems/maximum-number-of-vowels-in-a-substring-of-given-length/solutions/2809359/tao-lu-jiao-ni-jie-jue-ding-chang-hua-ch-fzfo/)

定长滑动窗口模板总结成三步：入-更新-出。

1. 入：下标为 i 的元素进入窗口，更新相关统计量。如果 i < k−1 则重复第一步。
2. 更新：更新答案。一般是更新最大值/最小值。
3. 出：下标为 i−k+1 的元素离开窗口，更新相关统计量。
以上三步适用于所有定长滑窗题目。

```js
let len = arr.length, ans = 0;
for (let i = 0; i < len; i++) {
  // 入
  // 。。。

  if (i < k - 1) {
    continue;
  }

  // 更新
  // ans = 。。。

  // 出
  // 。。。
}
```

### [1456. 定长子串中元音的最大数目](https://leetcode.cn/problems/maximum-number-of-vowels-in-a-substring-of-given-length/description/)

给你字符串 s 和整数 k 。

请返回字符串 s 中长度为 k 的单个子字符串中可能包含的最大元音字母数。

英文中的 元音字母 为（a, e, i, o, u）。

```js
/**
 * @param {string} s
 * @param {number} k
 * @return {number}
 */
var maxVowels = function (s, k) {
  let ans = 0,
    num = 0;
  for (let i = 0; i < s.length; i++) {
    // 入
    if (
      s[i] === "a" ||
      s[i] === "e" ||
      s[i] === "i" ||
      s[i] === "o" ||
      s[i] === "u"
    ) {
      num++;
    }

    if (i < k - 1) {
      continue;
    }

    // 更新
    ans = Math.max(ans, num);
    if (ans === k) {
      return ans;
    }

    // 出
    const out = s[i - k + 1];
    if (
      out === "a" ||
      out === "e" ||
      out === "i" ||
      out === "o" ||
      out === "u"
    ) {
      num--;
    }
  }

  return ans;
};
```
