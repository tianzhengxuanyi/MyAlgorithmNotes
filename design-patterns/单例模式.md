### 单例模式

#### 定义和针对场景
单例模式是一种创建型设计模式，它确保一个类只有一个实例，并提供一个全局访问点来访问该实例。

#### 核心思想
控制对象的实例化过程，确保只有一个实例被创建，从而节省资源并保证行为一致性。

#### 特定
- 唯一性
- 全局访问
- 延迟初始化

#### 实现方式
- 饿汉式
- 懒汉式
- 线程安全的懒汉式
- 枚举实现

#### 应用场景
- 缓存（如内存缓存）
- 全局状态管理（vuex、redux等）